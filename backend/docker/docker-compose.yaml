services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: metricforge
    env_file:
      - ../.env
    ports:
      - "127.0.0.1:8000:8000"
    restart: unless-stopped
