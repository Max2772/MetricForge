services:
  api:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: metricforge
    env_file:
      - ../.env
    ports:
      - "9000:9000"
    restart: unless-stopped